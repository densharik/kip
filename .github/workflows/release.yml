name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release
      - name: Stage artifact
        shell: bash
        run: |
          mkdir dist
          cp target/release/kip.exe dist/kip.exe
      - uses: actions/upload-artifact@v4
        with:
          name: kip-windows-x64
          path: dist/kip.exe

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin,aarch64-apple-darwin
      - uses: Swatinem/rust-cache@v2
      - name: Build universal binary
        run: |
          cargo build --release --target x86_64-apple-darwin
          cargo build --release --target aarch64-apple-darwin
          mkdir -p dist
          lipo -create -output dist/kip \
            target/x86_64-apple-darwin/release/kip \
            target/aarch64-apple-darwin/release/kip
      - name: Bundle kip.app
        run: |
          APP=dist/kip.app
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          cp dist/kip "$APP/Contents/MacOS/kip"
          chmod +x "$APP/Contents/MacOS/kip"
          cp resources/kip-ctx-hook.sh "$APP/Contents/Resources/"
          cp resources/icon.icns "$APP/Contents/Resources/kip.icns"
          cat > "$APP/Contents/Info.plist" <<'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleName</key><string>kip</string>
            <key>CFBundleDisplayName</key><string>kip</string>
            <key>CFBundleIdentifier</key><string>com.densharik.kip</string>
            <key>CFBundleVersion</key><string>0.1.0</string>
            <key>CFBundleShortVersionString</key><string>0.1.0</string>
            <key>CFBundleExecutable</key><string>kip</string>
            <key>CFBundleIconFile</key><string>kip</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>LSMinimumSystemVersion</key><string>11.0</string>
            <key>NSHighResolutionCapable</key><true/>
          </dict>
          </plist>
          PLIST
          codesign --force --deep -s - "$APP"
          ditto -c -k --keepParent "$APP" dist/kip-macos.zip
      - uses: actions/upload-artifact@v4
        with:
          name: kip-macos
          path: dist/kip-macos.zip

  release:
    needs: [windows, macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/kip-windows-x64/kip.exe
            artifacts/kip-macos/kip-macos.zip
          generate_release_notes: true
